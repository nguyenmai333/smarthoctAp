version: '3.9'
services:
  my_app:
    build: .
    image: uit-docker-app
    container_name: uit-docker-app
    ports:
      - "9000:8000"
    volumes:
      - .:/app
    environment:
      - PYTHONUNBUFFERED=1
