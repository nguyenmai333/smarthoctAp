<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.StudyManagement" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="ModernSeekBar" parent="Widget.AppCompat.SeekBar">
        <item name="android:thumb">@drawable/custom_thumb</item>
        <item name="android:progressDrawable">@drawable/custom_progress</item>
        <item name="android:minHeight">4dp</item>
        <item name="android:maxHeight">4dp</item>
    </style>

    <style name="Theme.StudyManagement" parent="Base.Theme.StudyManagement" />
</resources>